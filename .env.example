# Copy to ".env" for local testing of the live AI chat.
# Leave unset to use the built-in offline keyword-retrieval fallback.
#
# After deploying the Cloudflare Worker (see worker/README.md), put its URL here:
VITE_CHAT_API_URL=
